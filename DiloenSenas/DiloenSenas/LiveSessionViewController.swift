//
//  LiveSessionViewController.swift
//  DiloenSenas
//
//  Created by Alumno on 16/10/23.
//


import Foundation
import SwiftUI

/*struct LiveSessionViewController: UIViewControllerRepresentable {
    typealias UIViewControllerType = UIViewController

    func makeUIViewController(context: Context) -> UIViewController {
        // Crea e instancia el ViewModel
        let viewModel = LiveSessionViewModel()
        
        // Crea la vista LiveSession y pasa el ViewModel
        //let liveSessionView = LiveSession(classifier: viewModel.imageClassifier)
        
        // Crea un UIHostingController para la vista LiveSession
        //let hostingController = UIHostingController(rootView: liveSessionView)
        
        //return hostingController
        return nil
    }

    func updateUIViewController(_ uiViewController: UIViewController, context: Context) {
        // No es necesario realizar actualizaciones en este caso
    }
}
*/
